#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

source "$ROOT_DIR/scripts/toolbox_env.sh"

echo "Promotion script placeholder."
echo "Only use this after the local Strix Halo phase gate passes."
