name: mathy
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - matplotlib>=3.8
      - networkx>=3.2
      - numpy>=1.26
      - pandas>=2.2
      - pyarrow>=15.0
      - pyyaml>=6.0
      - sympy>=1.13
      - torch>=2.4

